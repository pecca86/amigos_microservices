
server:
  port: 8761
eureka:
  client:
    fetch-registry: false
    register-with-eureka: false # false, since this is the server
spring:
  application:
    name: eureka-server
  zipkin:
    base-url: http://localhost:9411/